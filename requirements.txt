# --- App / UI ---
streamlit

# --- Data stack (compatible wheels on Linux x86_64, Python 3.11) ---
pandas
numpy
openpyxl
scikit-learn

# --- HF / inference stack ---
huggingface_hub
transformers
tokenizers
safetensors
pyzmq

# --- PyTorch CPU (matches Py 3.11 wheels) ---
torch

# --- ConTextTab (pulls latest, needs pandas>=2.2.3 & sklearn>=1.6.1) ---
git+https://github.com/SAP-samples/contexttab